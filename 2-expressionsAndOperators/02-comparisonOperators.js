// Equality Operator

    // equl operator
    '3' == 3; // -> true

    // strict eequality operator
    '3' === 3; // -> false

    // Opposite operator

    '3' != 3; // -> false // Not, or Bang, or Exclamation point

    '3' !== 3; // -> true

    // Great than

    3 > 4; // -> false

    // Less than
    3 < 4; // -> true


    // Great than or equal to
    3 >= 4; // ->false

    // Less than or equal to
    3 <=4; 

    // AND operator
    let x = 13;
    x >= 5 && x <= 33;
    
    // OR operator
    x > 3 || x == -3

    <= 8, !5
    let y = 1
    y <= 8 && y !== 5


